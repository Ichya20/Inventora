rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic rules for prototyping: allow read/write for now
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
